generated: '2026-08-09'
method: searched
source: https://pypi.org/pypi/atmospore/json
description: >-
  First-party client libraries for the Atmospore Pollen Forecast API. Two packages are
  published from the atmospore GitHub organization; only the async Python client is
  actually released to a public registry.
packages:
  - language: python
    registry: pypi
    name: atmospore
    version: 0.1.0
    url: https://pypi.org/project/atmospore/
    install: pip install atmospore
    repository: https://github.com/atmospore/atmospore-python
    license: MIT
    official: true
    description: >-
      Async Python client (aiohttp + pydantic) wrapping /v1/pollen, /v1/pollen-top,
      /v1/pollen-area and /v1/species. Typed models, 5xx retry with exponential backoff,
      normalized risk levels, and typed exceptions (AuthenticationError, RateLimitError,
      APIError, AtmosporeError). RateLimitError carries .limit/.used/.resets_at.
    requires_python: '>=3.10'
    x-evidence:
      fetched: '2026-08-09'
      url: https://pypi.org/pypi/atmospore/json
      http_status: 200
  - language: python
    registry: none
    name: atmospore-mcp
    version: 0.1.0
    url: https://github.com/atmospore/atmospore-mcp
    install: pip install -e git+https://github.com/atmospore/atmospore-mcp#egg=atmospore-mcp
    repository: https://github.com/atmospore/atmospore-mcp
    license: MIT
    official: true
    published: false
    description: >-
      MCP (Model Context Protocol) server exposing four pollen tools over stdio. Depends on
      atmospore>=0.1.0 and mcp>=1.2. Console script `atmospore-mcp`.
    note: >-
      The repository README instructs `pip install atmospore-mcp`, but no such project
      exists on PyPI as of 2026-08-09 (https://pypi.org/pypi/atmospore-mcp/json -> 404).
      Recorded as source-only; the documented install command does not currently resolve.
    x-evidence:
      fetched: '2026-08-09'
      url: https://pypi.org/pypi/atmospore-mcp/json
      http_status: 404
registries_probed:
  - {registry: pypi, query: atmospore, status: 200}
  - {registry: pypi, query: atmospore-mcp, status: 404}
  - {registry: pypi, query: atmospore-python, status: 404}
  - {registry: npm, query: atmospore, status: 404}
  - {registry: npm, query: atmospore-mcp, status: 404}
  - {registry: npm, query: '@atmospore/sdk', status: 404}
gaps:
  - No JavaScript/TypeScript client, despite the embeddable widget being browser-delivered.
  - atmospore-mcp is not published to PyPI, so the documented install command fails.
